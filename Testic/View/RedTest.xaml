<Page x:Class="Testic.RedTest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewmodel="clr-namespace:Testic.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:RedTestViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" 
      d:DesignWidth="800"
      Title="RedTest">
    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding quest}" Grid.ColumnSpan="2" Grid.Row="0"/>
        <Button Content="Добавить строку" Grid.Column="0" Grid.Row="1" Command="{Binding Command22}" />
        <Button Content="Сохранить тест"  Grid.Column="1" Grid.Row="1" Command="{Binding Command33}"/>
   </Grid>
</Page>
