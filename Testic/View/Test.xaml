<Page x:Class="Testic.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Testic" xmlns:viewmodel="clr-namespace:Testic.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Test">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="1" Grid.Row="2" Content="{Binding first}" Command="{Binding Command1}"/>
        <Button Grid.Column="1" Grid.Row="3" Content="{Binding second}" Command="{Binding Command2}"/>
        <Button Grid.Column="1" Grid.Row="4" Content="{Binding _3}" Command="{Binding Command3}"/>
        <TextBlock TextWrapping="Wrap" Grid.Column="1" Grid.Row="1" Text="{Binding Description}"/>
        <TextBlock TextWrapping="Wrap" Grid.Column="1" Grid.Row="0" Text="{Binding Title}"/>
    </Grid>
</Page>
